<h1> All Students </h1>
<% @students.each do |student| %>
  <p> <%= student.name %></p>
  <p> <%= student.age %></p>
  <p> <%= student.house %></p>
<% end %>
<p>Average Age: <%= @students.average_age %></p>
